import React from "react";
import classes from "./Settings.module.css";

const Settings = () => {
    fetch("", { method: "GET" });
    return <div className={classes.settingsTitle}>Settings</div>;
};

export default Settings;
